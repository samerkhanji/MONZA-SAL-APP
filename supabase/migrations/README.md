# Supabase Migrations

**Do not run any migrations from this project.**

The database is already set up and managed directly in the Supabase Dashboard. All tables, views, functions, enums, and RLS policies exist in production.

The app connects to Supabase as a **client only** — it never creates or modifies the database schema.
